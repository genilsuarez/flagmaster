# Tasks

- [x] Refactor AppRouter to support only Home and Game screens: Update `AppRouter.SCREENS` from `['landing', 'modeSelector', 'parametrization', 'game']` to `['home', 'game']`, change the default screen in the constructor from `'landing'` to `'home'`, update `_cacheContainers()` to reference `home: document.getElementById('homeScreen')` and `game: document.querySelector('.game-wrapper')`, remove the legacy `landing-mode` body class management from `_updateBodyClass()`, update `back()` to return `'home'` instead of `'landing'` when history is empty, update `reset()` default parameter from `'landing'` to `'home'`, add `_initPopstateListener()` method that listens for browser `popstate` events and calls `reset('home')` when on the game screen, call `history.pushState()` in `navigate()` to integrate with browser History API, add CSS transition classes for fade-in/fade-out animations (300ms, cubic-bezier(0.4, 0, 0.2, 1)) with `prefers-reduced-motion` support, update `AppRouter.test.js` to reflect the new screen list and behavior.
- [x] Create ModeOptions model: Create `src/models/ModeOptions.js` exporting a `MODE_OPTIONS` constant, define option schemas per mode: `{ id, label, type, default, min?, max?, options? }`, include options for all 8 modes with their specific configuration fields, export a helper function `getOptionsForMode(modeId)` that returns the relevant options array.
- [x] Create ModeCardView component: Create `src/views/ModeCardView.js` with a `ModeCardView` class, implement `render()` that returns an `<article>` element with `role="button"`, `tabindex="0"`, and `aria-label` in format "{name} — {category}: {description}", render card content: emoji icon (with `aria-hidden="true"`), mode name (`<h3>`, DM Serif Display), description (`<p>`, Inter), category badge, style badge with sage (#7a9b7f) background for team modes and ocean (#6b8fa3) for individual modes, add hover/focus interaction: `translateY(-3px)` with 300ms transition using `cubic-bezier(0.4, 0, 0.2, 1)`, ensure minimum 44x44px touch target, add click and keyboard (Enter/Space) event handlers that call `onSelect(modeId)`, add responsive behavior: hide description text on viewports < 768px via CSS class.
- [x] Create BottomSheetView component: Create `src/views/BottomSheetView.js` with a `BottomSheetView` class, implement `open(modeId)` method: creates backdrop overlay, renders sheet panel with slide-up animation (300ms), sets `role="dialog"` and `aria-modal="true"`, implement `close()` method: slide-down animation (300ms), removes DOM elements, restores focus to triggering element, implement focus trap: Tab cycles within the sheet, Shift+Tab wraps, Escape closes, render configuration controls based on mode category using `MODE_OPTIONS`, implement `_loadSavedConfig(modeId)` reading from `localStorage` key `flagquiz_mode_config_{modeId}`, implement `_saveConfig(modeId, config)` writing to `localStorage`, implement `_updatePlayButtonState()`: disable "Jugar" button and show message when filtered pool < 5 countries, add swipe-to-dismiss gesture support for touch devices, style with Editorial Luxe: warm-white background, 16px top border-radius, shadow-lifted, sage-colored "Jugar" button, implement backdrop click to dismiss.
- [x] Create EngagementSectionView component: Create `src/views/EngagementSectionView.js` with an `EngagementSectionView` class, implement `render()` that returns a section element positioned above mode cards, display streak badge: 🔥 icon + days count from `StatsService.getStats().currentStreak`, display global progress: "{uniqueCountriesCorrect} de {totalCountries}" banderas acertadas, display "last played" quick-replay button when user has previous sessions, handle empty state: hide streak and last-played when no sessions exist, show only "0 de {total}" progress, handle invalid/missing localStorage data gracefully (show defaults, no errors), implement `update()` method to refresh stats without full re-render, wire `onQuickPlay(modeId)` callback for the replay button.
- [x] Create HomeView component with mode cards grid: Create `src/views/HomeView.js` with a `HomeView` class that renders the full home screen, implement `render()` method that creates: header (logo, streak badge, menu button), engagement section, team modes section, individual modes section, read modes from `GAME_MODES` and group by `category` field ('team' first, 'individual' second), render section headings "Modos en Equipo" and "Modos Individuales" using `<h2>` with DM Serif Display font, add ARIA landmarks: `role="main"` on content wrapper, `role="region"` with `aria-label` on each category section, instantiate `ModeCardView` for each mode and `EngagementSectionView` for stats, implement `update()` method that refreshes engagement data without full re-render, implement `destroy()` method for cleanup, wire `onModeSelect` callback that receives the selected `modeId`.
- [x] Update index.html structure: Replace the `<section id="landingHero">` with a new `<main id="homeScreen">` container, move the nav header (logo, menu button, streak badge) into the homeScreen as a sticky header, remove the landing hero content (title, subtitle, CTA button, aurora, flags grid, steps), remove `<div id="modeSelectorScreen">` element, remove `<div id="parametrizationScreen">` element, keep the app drawer, app modal, game wrapper, and legacy filter panel unchanged, change `<body class="landing-mode">` to `<body class="screen-home">`, add Bottom Sheet container: `<div id="bottomSheet" class="bottom-sheet" hidden></div>` before the game wrapper.
- [x] Add CSS styles for Home screen and Bottom Sheet: Add `.home-screen` styles: cream background (#faf8f5), max-width 640px centered, padding using design system spacing, add `.home-header` styles: sticky positioning, flex layout with logo/streak/menu, compact height, add `.engagement-section` styles: compact card layout, streak badge, progress indicator, quick-play button, add `.mode-section` styles: section headings (DM Serif Display), spacing between sections (16px+), add `.mode-card` styles: white background, shadow-soft, radius 8-12px, padding, flex column layout, add `.mode-card:hover` and `.mode-card:focus` styles: translateY(-3px), shadow-medium, transition 300ms ease, add `.mode-card__badge--team` (sage) and `.mode-card__badge--individual` (ocean) badge colors, add `.bottom-sheet` styles: fixed bottom, white background, radius-xl top corners, shadow-lifted, add `.bottom-sheet__backdrop` styles: semi-transparent overlay, fade-in animation, add slide-up/slide-down keyframe animations for Bottom Sheet (300ms, ease-gentle), add responsive styles: single column grid < 768px, hide descriptions on mobile, 2-column grid >= 768px, add `@media (prefers-reduced-motion: reduce)` rules: disable all transitions and animations.
- [x] Update main.js orchestration: Remove imports of `ModeSelectorView` and `ParametrizationView`, add imports for `HomeView`, `BottomSheetView`, `EngagementSectionView`, instantiate `HomeView` on DOMContentLoaded with callbacks: `onModeSelect` opens the BottomSheet, instantiate `BottomSheetView` with `onPlay` callback that calls `startGame()` and `onDismiss` that closes the sheet, update `app:navigate` event handler to only handle 'home' and 'game' screens, update `handleSessionEnd` to call `router.reset('home')` instead of `router.reset('landing')`, update `handlePlayAgain` to open the BottomSheet for the mode instead of navigating to parametrization, update `AppMenu` callbacks: `onHome` calls `router.reset('home')`, remove the `landingCTA` click listener, keep `startGame()` function logic intact (filtering, shuffling, session start).
- [x] Wire engagement data and quick-play flow: Add `lastPlayedMode` tracking to `StatsService`: save mode ID after each completed session, add `getLastPlayedMode()` method to `StatsService` that returns the last played mode ID or null, wire `EngagementSectionView.onQuickPlay` to open the BottomSheet pre-configured for that mode, ensure `HomeView.update()` is called after returning from a game session to refresh engagement stats, verify streak badge updates correctly when navigating back to home.
- [x] Remove deprecated views and clean up: Delete `src/views/ModeSelectorView.js` and `src/views/ModeSelectorView.test.js`, delete `src/views/ParametrizationView.js` and `src/views/ParametrizationView.test.js`, remove any remaining references to `modeSelectorView` and `parametrizationView` in the codebase, remove the `landingSettingsBtn` click listener (settings now accessible only via drawer), clean up unused CSS classes related to `.landing-hero`, `.landing-*`, `.mode-selector` (old grid styles), verify no console errors or broken references after cleanup.
- [x] Accessibility and keyboard navigation verification: Verify all ModeCards are reachable via Tab key and activable via Enter/Space, verify Bottom Sheet focus trap works: Tab cycles within, Escape closes and restores focus, verify ARIA landmarks are present: `role="main"`, `role="navigation"`, `role="region"` with labels, verify `aria-label` format on mode cards: "{name} — {categoría}: {description}", verify Bottom Sheet has `role="dialog"` and `aria-modal="true"` when open, verify color contrast ratios meet WCAG 2.1 AA (4.5:1 normal text, 3:1 large text), verify `prefers-reduced-motion` disables all animations.

## Task Dependency Graph

```json
{
  "waves": [
    [1, 2, 3, 5],
    [4, 6],
    [7, 8],
    [9],
    [10],
    [11],
    [12]
  ]
}
```
